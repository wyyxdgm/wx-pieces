<canvas
  type="webgl"
  id="webgl"
  style="width: {{width}}px; height: {{height}}px"
  bindtouchend="onTouchEnd"
>
    <cover-view class="info fps">fps: {{fps}}</cover-view>
</canvas>

<button type="primary" bindtap="onResetPanel">重置平面</button>
